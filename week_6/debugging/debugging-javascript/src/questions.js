const QUESTIONS = [
  "console.log() is a good way to debug JavaScript",
  "It's a bad idea to take breaks before the bug is fixed",
  "If your code isn't working, you should try something at random to get it to work",
  "There is a Node debugger built into VSCode",
  "The two steps in effective debugging are Get Visibility, and Tighten the Loop",
];

module.exports = QUESTIONS;
